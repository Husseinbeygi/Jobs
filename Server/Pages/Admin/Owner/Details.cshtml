@page
@model Server.Pages.Admin.Owner.DetailsModel
@{
	var pageTitle =
	$"{Resources.ButtonCaptions.Details} {Resources.DataDictionary.Owner}";

	ViewData[Infrastructure.ViewDataKeyNames.PageTitle] = pageTitle;

	Layout = "Layouts/Admin/Rtl/_Layout";
}

<section-form>
	<fieldset disabled>
		<section-form-header>
			@(pageTitle)
		</section-form-header>

		<partial name="PartialViews/_DisplayPageMessages" />

		<section-form-field>
			<label asp-for="ViewModel.FName"></label>
			<input asp-for="ViewModel.FName" />
		</section-form-field>

		<section-form-field>
			<label asp-for="ViewModel.LName"></label>
			<input asp-for="ViewModel.LName" />
		</section-form-field>

		<section-form-field>
			<label asp-for="ViewModel.PhoneNumber"></label>
			<input asp-for="ViewModel.PhoneNumber" />
		</section-form-field>

		<section-form-check>
			<input asp-for="ViewModel.IsActive" />
			<label asp-for="ViewModel.IsActive"></label>
		</section-form-check>

		<section-form-check>
			<input asp-for="ViewModel.IsUndeletable" />
			<label asp-for="ViewModel.IsUndeletable"></label>
		</section-form-check>

	</fieldset>

	<section-form-footer>
		<a asp-page="Index">
			@(Html.DtatGetLinkCaptionForList())
		</a>
		<a asp-page="Update" asp-route-id="@(Model.ViewModel.Id)">
			@(Html.DtatGetLinkCaptionForUpdate())
		</a>
		<a asp-page="Delete" asp-route-id="@(Model.ViewModel.Id)">
			@(Html.DtatGetLinkCaptionForDelete())
		</a>
	</section-form-footer>
</section-form>